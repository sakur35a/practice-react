# Repository guidelines

## 커밋 메시지

- 커밋 메시지는 `<type>: <한국어 메시지>` 형식을 사용한다.
- `type`에는 변경 성격에 맞게 `feat`, `fix`, `docs`, `refactor`, `test`, `chore` 등을 사용한다.
- 예: `feat: Cloudflare Pages Functions 프록시 추가`
